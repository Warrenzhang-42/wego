# WeGO Project Rules: 原子化 AI 驱动开发指令 (Atomic AI-Driven Development)

**回复原则 (Communication)**：AI 在所有对话中必须 **始终使用中文**。

本文件定义了当前项目（WeGO）开发过程中的核心原则。所有的 AI Agent 在处理需求时，**必须绝对遵守**以下规则。

## 一、 核心心法 (Core Philosophy)
- **极度收敛**：把 AI 当作“记忆力极短、执行力极强的高级外包”，依赖原子化任务驱动。
- **强制克隆**：在修改代码前分析现有范式，并严格克隆已有的代码审美与逻辑结构。
- **环境自愈**：AI 可以在必要时运行验证脚本或检查服务器状态以确保交付质量。

## 二、 架构原则：四层物理解耦
本项目严格拆分为 4 个物理隔离的模块，开发时必须保持绝对的低耦合：
1. **前端层 (`src/`)**：前端是“傻瓜”，只负责纯粹的 UI 渲染和事件分发，**禁止**在前端堆砌复杂的业务状态判断和坐标系杂糅。
2. **后端层 (`server/`)**：负责 Supabase Edge Functions、数据库 Migrations，作为前后盾交互与 AI Agent 的网关。
3. **AI 层 (`agent/`)**：负责 LangGraph 状态图、工具集 (`tools/`) 和 Prompts。AI 独立运行，仅通过 API 对外暴露。
4. **数据与契约层 (`data/` & `contracts/`)**：`contracts/` 下的 JSON Schema 是最高法律（裁判员），所有的输入输出格式必须严格符合契约。

## 三、 契约先行 (Contract-First)
- 在编写任何具体的逻辑代码前，首先确认或定义 `contracts/` 下的 JSON Schema（如：`route.schema.json`, `spot.schema.json`, `chat-message.schema.json` 等）。
- 所有跨层调用（例如 API 响应、AI 生成的结构化数据、前端接收的消息）**必须严格符合预先定义的契约**。不能多字段，也不能少字段。

## 四、 原子化任务与风格规范 (Atomic Tasks & Style)
- **单一原则**：一个 Task 只能指派“单一文件、单一函数、单一目的”。
- **风格克隆 (Style Cloning)**：执行修改前必须深度分析现有代码的风格（变量命名、组件结构、解构习惯、错误流等），并严格延续该模式。禁止引入任何与现有代码库审美不符的范式。
- **不得串改**：未明确指示修改的文件，**绝对不能**擅自修改或格式化其无关代码。

## 五、 AI 执行与评估模板
在接收开发指令或编写具体功能时，必须依照以下路径进行思考与执行验证：
1. **明确任务 (Task)**：当前要实现的“唯一”子功能是什么？
2. **确认约束 (Constraints)**：只能改动哪一个（或两个）指定文件？
3. **匹配契约 (I/O)**：检查 `contracts/` 下的 JSON Schema，确保字段完全吻合。
4. **自检清单 (Verification)**：
   - [ ] 坐标系防坑：数据库 WGS-84，前端渲染转 GCJ-02，严禁污染。
   - [ ] 逻辑注入：仅作功能注入，不在无提示的情况下摧毁纯手工 UI 设计。
   - [ ] 环境自愈：AI 允许在必要时检查或运行短期服务以验证修改结果，但应避免端口冲突。

## 六、 AI 与记忆基建（索引）
- 本机 **OpenClaw / 向量记忆** 的单一事实来源：`docs/openclaw-and-cursor-memory-runbook.md`。

## 七、 运行环境说明
- **标准端口**：前台 `5173`，Admin `5174`，API `8787`。
- **服务准则**：除非用户明确禁止，否则 AI 可辅助运行轻量验证脚本或检查服务器状态。

## 八、 开发日志（随时记录）
- **单一事实来源**：重要决策记录到 `docs/development-log.md`。
- **何时追加**：完成功能闭环或契约变更后，在同一次交付中在日志中录入变动记录。
